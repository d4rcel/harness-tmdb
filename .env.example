# Harness model API key (Google AI Studio). Never commit the real .env.
GEMINI_API_KEY=
# Leave empty to use the default (gemini-3.6-flash); the harness falls back
# through GEMINI_FALLBACK_MODELS when the primary is saturated.
GEMINI_MODEL=